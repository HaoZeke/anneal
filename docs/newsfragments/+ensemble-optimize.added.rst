Python ``ensemble_optimize`` runs the production hop ensemble. The
binding releases the GIL before replica threads start. A padded box
hop disables cluster symmetry and SOAP retries so a CUTEst cell
cannot spin without charging the ledger. Each Python eval and grad
callback spends one ledger unit.
