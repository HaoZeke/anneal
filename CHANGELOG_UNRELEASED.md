# Unreleased

## Cluster search, measured

- `Config::recommended(n)`: composed surface relocations (one acceptance
  test per excursion), Normal-Gamma Thompson allocation rewarded by depth,
  tabu on stall. Paired-control evidence across LJ38/55/75/98; reference
  GMIN at matched potential-call budgets solves 37/48, 0/48, 0/48 where
  this stack measures 55/72, 49/144 (BF 3104), 40/72.
- Burst move, depth-rewarded allocator, tabu composition; PT-over-comb
  measured equivalent to comb+tabu at LJ98 and not recommended (replica
  cost without ceiling gain).

## Infrastructure

- Canonical contact-graph basin identity (nauty), vertex-coloured with
  per-species-pair cutoffs (`graphkey` feature).
- vesin cell lists compiled from upstream as the neighbour backend
  (`vesin-nl` feature, `VESIN_SRC`); exact incremental reconciliation.
- Franzblau primitive-ring profiles; `Ledger::charge_frac` fractional
  pricing with first-call gradient audit; `Settle` staged-quench hook.

## Measured negatives, kept as evidence

- Flat-histogram acceptance, statistical temperature, well-tempered
  energy bias, soft-subspace and learned-covariance perturbations,
  nested-as-configured, archive-discovery acquisition, committor
  population, early-gated screen, staged-settle-as-wired. Numbers and
  mechanisms in docs/derivations/.
